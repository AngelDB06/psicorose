apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: psicorose-ingress
  namespace: psicorose
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
  - hosts:
    - psicorose.54.165.173.244.sslip.io
    secretName: psicorose-tls
  rules:
  - host: psicorose.54.165.173.244.sslip.io
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5000
      - path: /uploads
        pathType: Prefix
        backend:
          service:
            name: backend-service
            port:
              number: 5000
      - path: /
        pathType: Prefix
        backend:
          service:
            name: frontend-service
            port:
              number: 80
